# Navigation Links
#   To link a homepage widget, specify the URL as a hash `#` followed by the filename of the
#     desired widget in your `content/home/` folder.
#   The weight parameter defines the order that the links will appear in.
#
# Phase 2 note: Home + Research (index page and four research-line pages) now have real
# content. Team / Publications / News / Calls will be added back here as each section is
# rebuilt with real Juanes Lab content in the next phases (see project notes).

main:
  - name: Home
    url: '/'
    weight: 10
  - name: Research
    url: '/research/'
    weight: 20
  - name: Contact
    url: 'mailto:majuanes@cipf.es'
    weight: 30
